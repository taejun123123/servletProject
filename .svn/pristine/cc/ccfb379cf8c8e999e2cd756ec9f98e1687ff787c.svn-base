@charset "UTF-8";
.albumLink:hover {
	background: #fff;
	cursor: pointer;
	opacity: 80%; /* 투명도 */
}

.musicLink:hover {
	background: #fff;
	cursor: pointer;
}

.row {
	margin-bottom: 20px; /* 간격을 설정 */
}
.disabled {
	background: #f9f9f9;
	cursor: not-allowed;
}
/* 추가 스타일: 비공개 음원 클릭 방지 및 툴팁 */
.dataRow.disabled {
	background: #f9f9f9;
	cursor: not-allowed;
}
/* 모달 리스트 컨테이너의 기본 스타일 */
.moduleList {
/*     max-height: 1000px; /* 컨테이너의 최대 높이 설정 */
    padding: 15px; /* 패딩 추가 */
    /* border: 1px solid #ddd; /* 테두리 추가 */
    border-style: none;
    border-radius: 5px; /* 모서리 둥글게 */
    background-color: white; /* 배경색 설정 */
}

/* 모달 리스트 항목 스타일 (선택 사항) */
.moduleList .dataRow {
    margin-bottom: 10px; /* 항목 간 간격 추가 */
}

/* 캐러셀 스타일 */
.carousel-inner {
	cursor: pointer;
}
.carousel-control-prev-icon,
.carousel-control-next-icon {
    background-color: rgba(0, 0, 0, 0.8); /* 검정색, 불투명도 80% */
    border-radius: 50%; /* 원형 버튼 */
    width: 50px; /* 버튼 크기 */
    height: 50px; /* 버튼 크기 */
    background-size: 20px 20px; /* 아이콘 크기 */
    background-position: center; /* 아이콘을 가운데에 위치 */
    background-repeat: no-repeat;
}

.carousel-control-prev-icon {
    background-image: url('data:image/svg+xml;charset=UTF-8,<svg xmlns="http://www.w3.org/2000/svg" fill="%23fff" viewBox="0 0 8 8"><path d="M4.29 7.71L0.59 4 4.29 0.29 5.71 1.71 3.41 4 5.71 6.29 4.29 7.71z"/></svg>');
}

.carousel-control-next-icon {
    background-image: url('data:image/svg+xml;charset=UTF-8,<svg xmlns="http://www.w3.org/2000/svg" fill="%23fff" viewBox="0 0 8 8"><path d="M3.71 7.71L7.41 4 3.71 0.29 2.29 1.71 4.59 4 2.29 6.29 3.71 7.71z"/></svg>');
}

.carousel-control-prev,
.carousel-control-next {
    width: 50px; /* 버튼 영역 크기 */
    height: 50px; /* 버튼 영역 크기 */
}

.carousel-control-prev:hover .carousel-control-prev-icon,
.carousel-control-next:hover .carousel-control-next-icon {
    background-color: rgba(0, 0, 0, 1); /* 호버시 불투명도 100% */
}
.carousel-item img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* 이미지 비율을 유지하면서 크기를 맞춤 */
}

.carousel-control-prev, .carousel-control-next {
    top: 50%;
    transform: translateY(-50%);
    width: 5%;
}

.carousel-control-prev-icon, .carousel-control-next-icon {
    background-color: rgba(0, 0, 0, 0.8);
    border-radius: 50%;
    padding: 10px;
}

